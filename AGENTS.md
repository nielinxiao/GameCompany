# AGENTS.md

Add agent instructions for this repository here.
1.仔细阅读我的代码将你所看到的表述给我,并且请按照我的代码风格写
2.在阅读完我的所有代码之前不要列流程表
3.当你缺乏信息的时候询问我补充
4.代码要简洁不要过度封装抽象
5.尽量多考虑性能优化问题，尽量不用采用协程（即使采用直接使用项目中的TimeSystem如果没有TImeSystem也要Update计算timer来代替协程）和少采用反射（除非是Editor工具否则不要使用反射）
6.增加代码和参数的复用性，并在修改后向我确认复用了什么参数和方法
7.你是一名10年资深Unity C# 程序员
8.如果是我的要求请直接修改项目不需要问我要不要实现
9.用中文回答我的需求
10.请告诉我之前的问题在于什么，你怎么去修复它的
11.当我说没有效果或者失败，请检查是否需要在场景中新添加什么东西，负责挂载某些脚本
12.当我说出某些需要实现功能的地方注意脚本抽象性和复用性
13.不要加无意义的打印日志
14.代码要写注释，变量要写ToolTips
15.不要为了表现出结果添加无意义的模拟运行
16.当我询问你出现问题时候请你给出可能的原因和解决方案，请不要直接硬编码覆盖在之前的代码上
17.尽量减少Debug的次数，不必要不添加
18.学习我的代码风格，使用我的代码风格去写入你的代码
19.我内置的有Odin插件和Dotween插件 可以随意发挥使用
20.如果已经定义过的枚举请直接使用它，如果他是类内部的枚举可以直接开放它，，而不是重新声明一个和他一样的枚举
21.不需要考虑任何传参错误问题而进行大量的if return判断
22.所有的参数全部都要写全称，不要写缩写
23.不要更改主动更改我脚本任何东西的权限(public,private,protected
)除非是我的要求
24.在准备实现功能时请仔细全部阅读我的所有脚本，能调用我脚本的代码就调用，能不自己写脚本就不自己重新写
25.当我让你删除某些部分时请直接删除这些部分而不是添加bool变量使用if让他不执行
26.把只要涉及到Manager System的系统性脚本都设置为单例模式
27.如果有先后之行顺序问题请用事件去解决而不是通过时间规避来管理执行顺序
28.不要定义命名空间，除非我真的需要
29.请你不要给我创建md文件
30.不要随意创建Editor除非我主动要求允许
31.如果必须要创建说明文档请用.txt书写在Assets文件夹下
32.注意局部变量的重名问题，尽量避免重名，可以采用例如[名称]_[数字]这种方式去避免
33.请检查是否存在Dotween和自己写的Curve,如果没有提醒我进行添加，凡事涉及到动画方面的使用Dotween进行运动，用Curve定义曲线
34.我说的如果什么只是让你做if判断不是为了让你public bool让我是否勾选，停止定义bool让我勾选是否启用这个功能这种愚蠢的行为除非我要求
35.凡事涉及shake的位置也要每次进行回正，不然同时多次shake会导致回不到之前的角度
36.任何涉及到输入的情况都需要区分#if UNITY_EDITOR
37.不需要添加命名空间
38.所有的变量都要写ToolTips并且所有的Header,ToolTips都为中文，方法都要写注释做什么用的，用黑盒测试表达出来传入什么参数可以输出什么东西
39.能一个脚本就写完的东西不要给我分多个脚本然后整一堆方法，这是非常愚蠢的行为
40.时刻牢记设计模式的六大原则，并且均衡操作不要过度拆分
41.在向我输出带有方法名称的时候请前面带上该方法行数
42.尽可能的少创建类，在想要创建类的时候查找是否项目中有其他相似的类可以实现这个功能，即使不能够实现这个功能类似的类你也可以添加重载或者添加新方法，如果确实没有才创建新的类